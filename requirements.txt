# Core runtime
transformers==4.55.0
kernels==0.12.1

# Megatron + TransformerEngine for unified FP8 can be installed via
# Dockerfile build arg INSTALL_MEGATRON_STACK=1. Keep shared deps here.

# Miles runtime deps (subset; keeps versions compatible with Py3.12)
accelerate>=0.33.0
blobfile>=2.1.0
datasets>=2.20.0
httpx[http2]>=0.27.0
omegaconf>=2.3.0
pillow>=10.4.0
pybase64>=1.3.2
pylatexenc>=2.10
pyyaml>=6.0.1
ray[default]>=2.10.0
tensorboard>=2.17.0
wandb>=0.17.0
ring_flash_attn
qwen_vl_utils

# Rollout / serving
sglang[all]==0.5.7
sglang-router>=0.2.3
